rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The authenticated server API owns all reads and writes. Firebase client
    // sign-in alone never grants database access.
    match /{document=**} { allow read, write: if false; }
  }
}
